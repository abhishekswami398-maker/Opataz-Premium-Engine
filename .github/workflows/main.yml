name: Build Android APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Install System Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential ccache git python3-dev libffi-dev libssl-dev unzip autoconf automake libtool pkg-config cmake

    - name: Install Buildozer and Fixed Cython
      run: |
        pip install --upgrade pip
        # सबसे स्थिर बिल्ड के लिए cython का यह वर्जन जरूरी है
        pip install "cython<3.0.0" buildozer virtualenv

    - name: Auto-Generate Clean Buildozer Spec
      run: |
        rm -f buildozer.spec
        cat <<EOT >> buildozer.spec
        [app]
        title = Opataz AI
        package.name = opatazai
        package.domain = org.opataz
        source.dir = .
        source.include_exts = py,png,jpg,kv,atlas
        version = 1.0.0
        requirements = python3,kivy,kivymd,pillow
        orientation = portrait
        fullscreen = 1
        android.permissions = READ_EXTERNAL_STORAGE, WRITE_EXTERNAL_STORAGE, INTERNET
        android.api = 33
        android.minapi = 24
        android.ndk = 25b
        android.accept_sdk_license = True
        android.private_storage = True
        android.archs = armeabi-v7a, arm64-v8a
        android.allow_backup = True
        [buildozer]
        log_level = 2
        warn_on_root = 1
        EOT

    - name: Clear Pre-Installed GitHub NDK Path
      run: |
        # यहाँ हम गिटहब के गलत r27 NDK को पूरी तरह अनसेट कर रहे हैं ताकि लफड़ा खत्म हो जाए
        unset ANDROID_NDK_HOME
        unset ANDROID_NDK_ROOT
        rm -rf ~/.buildozer
        rm -rf .buildozer/

    - name: Build with Buildozer
      run: |
        # एनवायरनमेंट वेरिएबल्स को हटाकर शुद्ध रखना ताकि बिल्डोइज़र खुद अपना सही r25b सेट करे
        export JAVA_HOME=$JAVA_HOME_17_X64
        export PATH=$JAVA_HOME/bin:$PATH
        unset ANDROID_NDK_HOME
        unset ANDROID_NDK_ROOT
        YES=1 buildozer -v android debug

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: Opataz-AI-APK
        path: bin/*.apk
        
